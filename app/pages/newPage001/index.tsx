import React from 'react';
import NewPage001Design, { newPage001Title, newPage001HeaderBarClassList } from '@smartface-generated/pages/newPage001';

export const NewPage001Options = { title: newPage001Title, headerConfiguration: newPage001HeaderBarClassList };

export default (props: any) => {
  return <NewPage001Design />;
};
