import $$StackLibrary from 'generated/router/components/StackLibrary';

export default class StackLibrary extends $$StackLibrary {
  constructor(basePath: string, name: string, routeIndex: number) {
    super(basePath);
  }
}
